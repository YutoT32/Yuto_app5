FROM node:22-alpine

WORKDIR /app

# package.jsonとpackage-lock.json（存在する場合）をコピー
COPY api/package*.json ./

# 依存関係をインストール
RUN npm install

# ポートを公開
EXPOSE 3000

# 開発モードでアプリケーションを起動（ホットリロード対応）
CMD ["npm", "run", "dev"]